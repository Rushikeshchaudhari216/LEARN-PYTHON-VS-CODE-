class programmer:
          company = "Tesla"
          def __init__(self, name, salary, pin, number ):
                  self.name = name
                  self.salary = salary
                  self.pin = pin
                  self.number = number

p = programmer("Rushi", 720000, 423303, 8390041707)
print(p.name, p.salary, p.pin, p.number, p.company)